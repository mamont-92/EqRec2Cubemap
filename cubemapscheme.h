#ifndef CUBEMAPSCHEME_H
#define CUBEMAPSCHEME_H

#include <QtCore>
#include <QObject>

enum class Scheme{
        VerticalCross,
        HorizontalCross,
        VerticalLine,
        HorizontalLine
    };
Q_ENUMS(Scheme)

#endif // CUBEMAPSCHEME_H
